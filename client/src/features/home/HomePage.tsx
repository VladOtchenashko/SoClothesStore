import { LinearProgress, Typography } from "@mui/material";

export default function HomePage() {
    return (
        <LinearProgress color="primary" variant="indeterminate" sx={{mt: 25}} />       
    )
}